# homebrew-tap

Homebrew tap for [ping-uin](https://github.com/altosaxplayer/ping-uin).

```bash
brew tap altosaxplayer/tap
brew install ping-uin
```

Or directly:

```bash
brew install altosaxplayer/tap/ping-uin
```
